# Math fixture: square power (mirrors test/unit/math-retrieval.test.ts DOCS.squarePower, L1 ratified; last sentence added so the ratified `x² + y² = r²` query vector anchors)

## Square Power

Polynomial expansion often writes the square of a variable as x² in plain text. Expanding x² + y² over the reals gives a circle of radius one when the equation is set equal to one in geometry class. The circle equation itself is x² + y² = r².
