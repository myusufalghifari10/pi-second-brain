# Math fixture: greek inequality (L1 canonicalization vector `α ≤ β` / `\alpha \leq \beta`, symmetry-pair of docs/layer1-implementation-report.md (b))

## Greek Inequality

In mathematical analysis the ordering between Greek variables is displayed as $$\alpha \leq \beta$$ and in plain text the same relation appears as α ≤ β between the letters.
