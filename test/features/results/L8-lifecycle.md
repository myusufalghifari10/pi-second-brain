# L8-lifecycle — Feature Test Results (H5–H8 + A7/A9 re-check)

Lane: L8-lifecycle · Engine: real built `dist/src/engine.js` (repo @ d7da3cb) · Node v26.8.1
Isolated stores: `/tmp/fx-L8-lifecycle/run-<feat>-<pid>` (fresh per probe, `PI_KNOWLEDGE_DIR` env)
Models: offline via `PI_KNOWLEDGE_MODEL_CACHE_DIR=/home/yusuf/.pi/knowledge/models`
Probe scripts (kept): `/tmp/fx-L8-lifecycle/probe-h5.mjs`, `probe-h6.mjs` + `run-h6.sh`, `probe-h7.mjs`, `probe-h7-negative.mjs`, `probe-h8.mjs`, `probe-h8-focus.mjs`, `probe-a7.mjs`, `probe-a9.mjs`, `env.sh`
Scope note: this lane is a re-run after an interrupted previous attempt — all probes were re-executed fresh this session with new isolated stores; every verdict below is backed by this session's observed output. No `src/`, `test/`, or fixture files modified.
Grading note (H8): per campaign instruction the doctor-flagging clause is a KNOWN FAIL being fixed immediately after this lane; H8 is graded on the search-degrade + update-rebuild clauses only. The doctor observation is recorded verbatim as a side-note.

## Verdicts

```
H5 | PASS | fresh PI_KNOWLEDGE_DIR /tmp/fx-L8-lifecycle/run-h5-983118: engine.initialize + add -> status=ready chunkCount=1; raw sqlite `SELECT version FROM schema_version` = 8 === SCHEMA_VERSION const in dist/src/storage/sqlite.js; 17 core tables present (chunks, chunks_fts, formulas, indexing_jobs, knowledge_bases, label_edges, symbols, ...); re-open of same store clean (1 KB h5fresh ready, search H5ALPHA7788 -> 1 hit); zero migration errors on boot and re-open (probe-h5.mjs, exit 0)
H6 | PASS | `ps aux | grep model-worker` BEFORE probe: "(no model-worker processes)"; during 30 hybrid/deep search rounds exactly ONE worker (`/usr/bin/node .../dist/src/model-worker.js`, pid 983330, child of probe); RSS samples KB [1110176, 1198480, 1261524, 1285940, 1333000, 1376812, 1426728] -> segment growth 148MB (q1-10, model warmup) -> 59MB (q11-20) -> 82MB (q21-30), decelerating = bounded, no unbounded growth, no per-query process spawn; engine.dispose() -> worker pid 983330 dead=true, remaining=[] ; `ps aux | grep model-worker` AFTER probe exit: "(no model-worker processes — no orphans)" (run-h6.sh + probe-h6.mjs, exit 0)
H7 | PASS | PI_KNOWLEDGE_AUTO_INJECT=true + real dist/index.js factory: `context` handler registered (events session_start, session_shutdown, context, before_agent_start); KB added via the REAL registered knowledge_add tool ("Indexed h7docs: 3 chunks from 3 files"); firing handlers.context with user msg "kubernetes scheduler binds pods nodes" -> injection.messages[0].content starts "[Knowledge context]" and contains "kube-scheduler" + "kubernetes.md" snippet; original user message preserved (2 messages returned); edges: user text "hi" -> undefined (silent), no user message -> undefined; session_shutdown -> engine disposed, remaining model-worker processes = [] . Negative control (probe-h7-negative.mjs): WITHOUT the env var -> registered events [session_start, session_shutdown, before_agent_start], context handler ABSENT (dormant by default, opt-in contract honored)
H8 | PASS | (graded clauses only — doctor clause is the declared known-fail) vector file /tmp/fx-L8-lifecycle/run-h8-984078/vectors/cab85b3d-....bin 4616B after add; pre-delete hybrid "how do entangled particles correlate measurements" -> 1 hit reason=hybrid score=1.196 top=quantum.md (vector leg live). rm the vector file -> fast search "H8SRD3344" still works: 1 hit top=sourdough.md reason=bm25; hybrid search: NO crash, 1 hit, score degraded to 0.468 (lexical serving; probe-h8-focus.mjs confirms file ABSENT during AND after the search — search itself does not rebuild). engine.update("h8docs") -> {"added":3,"removed":3,"unchanged":0}, vector file recreated 4616B; post-heal hybrid score back to EXACTLY 1.196 === pre-delete fused value (vector leg restored). Side-record (KNOWN FAIL, not graded): engine.doctor() after delete -> {"health_score":100,"summary":"Knowledge system is healthy.","issues":[],"actions":[]} — doctor does NOT flag the missing vector file yet (fix in flight by worker). Caveat for reviewer: degraded hybrid results still carry match_reason "hybrid" and no warning field — degradation is graceful (no crash, bm25-served) but not explicitly signaled
A7 | PASS | plan(proj) on dir with nested/deep + 11MB big.txt + pic.png(binary) + .gitignore'd secret/ + .env + node_modules: scannable_files=3, skipped total=5 {suggested_excluded:3 (.env, node_modules, secret), oversized:1 (big.txt 11534336B), binary:1 (pic.png)}; plan on UNINITIALIZED engine creates NO store dir; plan on initialized engine: list()=[] (no KB) and vectors dir absent (zero mutation); include_suggested_text preview: scannable_files=6, still list()=[]; single-file plan: "1 scannable file, 11.0 MB source size — exceeds the 10 MB single-file cap; ingestion will fail" vs 50B file plans clean (probe-a7.mjs, exit 0)
A9 | PASS | boundary: plan(exact 10485760B file) -> "1 scannable file, 10.0 MB source size" (NOT flagged, strict > cap); plan(CAP+1) -> flagged "exceeds the 10 MB single-file cap"; add(over10mb.txt) threw exactly "File exceeds the 10 MB ingestion cap: .../over10mb.txt — split it or add its directory instead"; add(over10mb.pdf, CAP+4096 junk pdf) threw the same cap error (cap fires before PDF parsing); after both rejects list() shows only the later successful small add (a9small ready, 1 chunk), db rows kb=1 chunks=1, vectors dir has exactly 1 .bin — no partial KB rows, store not corrupted (probe-a9.mjs, exit 0)
```

5 PASS / 0 FAIL / 0 SKIP (H8 graded on 2 of 3 clauses per declared known-fail; doctor clause recorded above)

## Environment & commands

- Working dir for all probes: `/tmp/fx-L8-lifecycle/`; each probe sets its own fresh `PI_KNOWLEDGE_DIR=/tmp/fx-L8-lifecycle/run-<feat>-<pid>` and imports `/home/yusuf/pi-knowledge/dist/src/engine.js` (real built engine, ESM).
- H5: `node probe-h5.mjs` — exit 0.
- H6: `bash run-h6.sh` (wraps `timeout 300 node probe-h6.mjs` between before/after `ps aux | grep model-worker` captures) — exit 0.
- H7: `node probe-h7.mjs` then `node probe-h7-negative.mjs` — both exit 0.
- H8: `node probe-h8.mjs` then `node probe-h8-focus.mjs` (tight per-step vector-file existence checks) — both exit 0.
- A7: `node probe-a7.mjs` — exit 0. A9: `node probe-a9.mjs` — exit 0.
- All searches ran against cached offline models (`PI_KNOWLEDGE_MODEL_CACHE_DIR=/home/yusuf/.pi/knowledge/models`); no network used.

## Observations (non-blocking)

1. **H6 RSS is bounded but not flat**: ~309MB total growth over 30 query rounds, decelerating per segment (148→59→82MB). The large first-segment growth is model warm-up inside the single persistent worker; no leak pattern (no per-query accumulation of processes, growth flattens). Flagged here in case a stricter "flat RSS" reading is wanted later.
2. **H7 conversational phrasing**: a stopword-heavy user message ("how does the scheduler bind pods to nodes in kubernetes?") yields no injection — fast-mode BM25 strict-AND finds nothing and the handler stays silent. Documented behavior gap for the auto-inject UX, not a contract violation (documented contract = KB hits injected when results exist; opt-in/-out verified).
3. **H8 degrade signal**: while degraded, hybrid results keep `match_reason: "hybrid"` with a BM25-only fused score (0.468 vs 1.196) and no warning field — a caller cannot distinguish "degraded" from "vector leg legitimately weak" without checking the doctor/vector file. This overlaps the doctor known-fail being fixed; recording both sides of the same defect.
4. **H8 search does not self-heal**: the focus probe confirms the vector file stays ABSENT after degraded searches — only `update()` rebuilds it (restored to the exact same 4616 bytes and identical fused score 1.196).
